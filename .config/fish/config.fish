# virtualfish for virtualenvs
# https://github.com/adambrenecki/virtualfish
eval (python -m virtualfish)

# Yarn global binaries
set -U fish_user_paths ~/.config/yarn/bin $fish_user_paths
set -U fish_user_paths ~/.config/yarn/global/node_modules/.bin $fish_user_paths
